apiVersion: kargo.akuity.io/v1alpha1
kind: PromotionTask
metadata:
  name: promote
  namespace: app-a
spec:
  vars:
  - name: repoURL
    value: https://github.com/zswanson/kargo-helm.git

  steps:
  - uses: git-clone
    config:
      repoURL: ${{ vars.repoURL }}
      checkout:
      - branch: main
        path: ./main
      - commit: ${{ commitFrom( vars.repoURL ).ID }}
        path: ./freight
      - branch: stage/${{ ctx.stage }}
        create: true
        path: ./out
  - uses: copy
    config:
      inPath: ./main/charts/guestbook
      outPath: ./out/app-a/charts/guestbook
  - uses: copy
    config:
      inPath: ./main/env-a/${{ ctx.stage }}/values.yaml
      outPath: ./out/app-a/values.yaml
  - uses: copy
    config:
      inPath: ./freight/base-a/feature-flags.yaml
      outPath: ./out/app-a/feature-flags.yaml
  - uses: git-commit
    as: commit
    config:
      path: ./out
      message: "promote app-a/${{ ctx.stage }} from ${{ commitFrom( vars.repoURL ).ID }}"
  - uses: git-push
    config:
      path: ./out
  - uses: argocd-wait
    config:
      apps:
      - name: app-a-${{ ctx.stage }}

---
apiVersion: kargo.akuity.io/v1alpha1
kind: PromotionTask
metadata:
  name: promote
  namespace: app-b
spec:
  vars:
  - name: repoURL
    value: https://github.com/zswanson/kargo-helm.git

  steps:
  - uses: git-clone
    config:
      repoURL: ${{ vars.repoURL }}
      checkout:
      - branch: main
        path: ./main
      - commit: ${{ commitFrom( vars.repoURL ).ID }}
        path: ./freight
      - branch: stage/${{ ctx.stage }}
        create: true
        path: ./out
  - uses: copy
    config:
      inPath: ./main/charts/guestbook
      outPath: ./out/app-b/charts/guestbook
  - uses: copy
    config:
      inPath: ./main/env-b/${{ ctx.stage }}/values.yaml
      outPath: ./out/app-b/values.yaml
  - uses: copy
    config:
      inPath: ./freight/base-b/feature-flags.yaml
      outPath: ./out/app-b/feature-flags.yaml
  - uses: git-commit
    as: commit
    config:
      path: ./out
      message: "promote app-b/${{ ctx.stage }} from ${{ commitFrom( vars.repoURL ).ID }}"
  - uses: git-push
    config:
      path: ./out
  - uses: argocd-wait
    config:
      apps:
      - name: app-b-${{ ctx.stage }}
